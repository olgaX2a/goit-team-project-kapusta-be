const addTransaction = require('./addTransaction');
const getTransactions = require('./getTransactions');
const deleteTransactions = require('./deleteTransactions');

module.exports = {
  addTransaction,
  getTransactions,
  deleteTransactions,
};
